# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = 0
    inner.vertical =   0
    outer.left =       0
    outer.bottom =     0
    outer.top =        0
    outer.right =      0

# Workspace to monitor assignment
# See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[workspace-to-monitor-force-assignment]
1 = 'LG HDR 4K'  # External monitor
2 = 'LG HDR 4K'
3 = 'LG HDR 4K'
4 = 'LG HDR 4K'
5 = 'LG HDR 4K'
6 = 'LG HDR 4K'
7 = 'LG HDR 4K'
8 = 'LG HDR 4K'
9 = 'LG HDR 4K'
10 = 'Built-in Retina Display'  # Laptop screen only

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    alt-comma = 'layout accordion horizontal vertical'

    # LAUNCHERS
    alt-e = 'exec-and-forget open -a Finder'  # Open Finder
    alt-o = 'exec-and-forget open -n -a "Brave Browser"'  # Open browser
    alt-u = 'exec-and-forget open -a "Brave Browser" "https://chatgpt.com/?temporary-chat=true"'  # ChatGPT
    alt-i = 'exec-and-forget open -a "System Settings"'  # System Settings
    alt-enter = 'exec-and-forget open -n -a Alacritty'  # Terminal
    alt-slash = 'exec-and-forget open -a Raycast'  # App launcher (Raycast is macOS equivalent of rofi)
    alt-shift-s = 'exec-and-forget screencapture -i ~/Pictures/Screenshots/screenshot_$(date +%Y-%m-%d_%H-%M-%S).png'  # Screenshot
    
    # WINDOW MANAGEMENT
    alt-f = 'fullscreen'  # Toggle fullscreen
    alt-shift-q = 'close'  # Kill window
    
    alt-h = 'focus left'
    alt-j = 'focus down'
    alt-k = 'focus up'
    alt-l = 'focus right'

    alt-shift-h = 'move --boundaries all-monitors-outer-frame --boundaries-action stop left'
    alt-shift-j = 'move --boundaries all-monitors-outer-frame --boundaries-action stop down'
    alt-shift-k = 'move --boundaries all-monitors-outer-frame --boundaries-action stop up'
    alt-shift-l = 'move --boundaries all-monitors-outer-frame --boundaries-action stop right'

    alt-minus = 'resize smart -50'
    alt-equal = 'resize smart +50'

    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'
    alt-7 = 'workspace 7'
    alt-8 = 'workspace 8'
    alt-9 = 'workspace 9'
    alt-0 = 'workspace 10'
    
    alt-n = 'exec-and-forget /Users/chiyonn/.config/aerospace/scripts/workspace-next-nonempty.sh'
    alt-p = 'exec-and-forget /Users/chiyonn/.config/aerospace/scripts/workspace-prev-nonempty.sh'

    alt-shift-1 = 'move-node-to-workspace 1'
    alt-shift-2 = 'move-node-to-workspace 2'
    alt-shift-3 = 'move-node-to-workspace 3'
    alt-shift-4 = 'move-node-to-workspace 4'
    alt-shift-5 = 'move-node-to-workspace 5'
    alt-shift-6 = 'move-node-to-workspace 6'
    alt-shift-7 = 'move-node-to-workspace 7'
    alt-shift-8 = 'move-node-to-workspace 8'
    alt-shift-9 = 'move-node-to-workspace 9'
    alt-shift-0 = 'move-node-to-workspace 10'

    alt-tab = 'focus-monitor --wrap-around next'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    alt-m = 'mode audio'  # Enter audio mode for volume/playback controls

    alt-shift-r = ['reload-config', 'mode main']  # Reload configuration

# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.audio.binding]
    # Volume controls
    j = 'exec-and-forget osascript -e "set volume output volume ((output volume of (get volume settings)) - 5)"'  # Volume down 5%
    k = 'exec-and-forget osascript -e "set volume output volume ((output volume of (get volume settings)) + 5)"'  # Volume up 5%
    
    # Exit audio mode
    enter = 'mode main'
    esc = 'mode main'
    alt-m = 'mode main'

# App-specific configurations
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'jp.naver.line.mac'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.tencent.xinWeChat'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.Dictionary'
run = 'layout floating'
